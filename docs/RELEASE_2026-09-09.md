# EcoGlobe release — 2026-09-09

User authorization: commit, push, and deploy all changes.

## Sources and reconciliation

- `a865ae6`: all previously uncommitted marketplace corrections, pricing evidence, lab referral workflow, screenshot form, documents and maintenance work.
- Merge source: `origin/claude/peaceful-heyrovsky-284108` at `01a0e593ea42458a78186791bc307ee7e8179158`, the existing hosted feature baseline. Its payment, logistics, account/team, documents, favorites, interest, saved searches, reports, registration and sample-conversion functionality is preserved.
- Persisted SQL listing data replaces static catalog copies. Unknown values remain unknown. The workbook prices remain saved in the shared database; synthetic local fixtures are separate.
- Lab panel examples remain internal drafts. No laboratory booking or external notification was added.
- Listing read/write adapters preserve legacy document references and recorded units. Anonymous/explorer listing detail is redacted according to the existing live access policy.
- Existing base schema and sample migration hashes are preserved. Incoming dedicated feature migrations supply the additions also present in the other branch's schema. The redundant sample conversion migration is omitted because the existing sample migration already adds that column after creating its table.

## Verification before deployment

- 22 runtime isolation tests, 17 backend unit tests, backend types passed.
- Marketplace SQL integration passed against the dedicated retained local database: onboarding, persisted fields and prices, document bytes, authorization, listing lifecycle and quote checks.
- Lab SQL integration passed: draft privacy, request validation and idempotency, admin queue and notes, notifications, actual PDF storage, private/shared/revoked access, linked samples, sample-to-order conversion, favorites/interest routes, combined onboarding response and reports.
- Initial merged frontend: web/admin types, test types, 20 unit tests and 168-route production build passed; lint had no errors and 54 existing warnings. Final compatibility checks are recorded below when complete.
- Shared database preflight confirmed `sqldb-ecoglobe-dev`, existing sample table/conversion column and one active internal admin membership with `admin_override`.
- Release migration is transactional and compares all listing prices, units, quantities and MOQ before/after. No database reset or seed is performed.

## Release result

Shared Azure SQL migration completed: both 20260909 migrations committed atomically, all 23 existing listing price/quantity/unit/MOQ rows preserved, four draft panels retained. Backend/web traffic switch pending final review. Earlier local reports remain historical evidence for their recorded file hashes, not verification of this merged release.

## Final review and local browser

Claude completed three bounded frontend integration/review tasks with no remaining release blockers; the coordinator reviewed and integrated the exact files. Final shared/web/admin type checks and 21 frontend unit tests passed. Backend review fixes preserve null inventory, infer order units from saved records, preserve omitted licence tiers, restrict technical document downloads to members, and prohibit non-admin internal permission assignment. Both SQL suites passed after these changes. The merged listing page was reloaded in the in-app browser and showed persisted specifications, technical documents, reports and the lab request entry point.

Final frontend changes are options/types and onboarding tier prefill following the passing 168-route build; the hosted production build is the final build gate. Existing lint warnings remain documented. No external laboratory, payment-provider or email delivery is certified by these checks.
